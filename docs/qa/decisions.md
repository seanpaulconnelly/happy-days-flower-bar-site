# Orchestrator decisions

Decisions made by the orchestrating session during the 2026-08-29 build that are not already in the build plan. Newest at the bottom.

| #   | Date       | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Why                                                                                                                                                                                                                                                                                                          |
| --- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D1  | 2026-08-29 | Design tokens live in `src/styles/theme.css` (owned by `ui-designer`) and are imported by `src/styles/index.css` (owned by `frontend-engineer`) rather than both agents editing one file.                                                                                                                                                                                                                                                                                                                                                                           | Disjoint file ownership; lets Phase 2 (design) and Phase 3 (scaffold) run concurrently with no write race.                                                                                                                                                                                                   |
| D2  | 2026-08-29 | Facebook URL resolved from the share link to `https://www.facebook.com/happydaysflowers` (`curl -sL` with the `facebookexternalhit/1.1` user agent; query parameters stripped).                                                                                                                                                                                                                                                                                                                                                                                     | R7. The plain browser user agent gets a 400 from the share endpoint; the crawler UA follows the redirect to the canonical page slug.                                                                                                                                                                         |
| D3  | 2026-08-29 | Punctuation in transcribed copy is normalised to typographic marks: `---` → `—`, `--` → `–`, straight apostrophes → `’` where the source is a pandoc-style markdown export. Words, order and spelling are untouched.                                                                                                                                                                                                                                                                                                                                                | The spec is a markdown export that encodes dashes as `---`/`--`; the owner's answers already use `’` and `–`. Rendering them literally would put mixed marks on one page. This is punctuation rendering, not paraphrase.                                                                                     |
| D4  | 2026-08-29 | `create-vite` 9.2 scaffolds with `.oxlintrc.json` (oxlint) instead of eslint. We replace it with eslint + typescript-eslint + prettier per the plan and the `lint-changed.sh` hook.                                                                                                                                                                                                                                                                                                                                                                                 | The PostToolUse hook and `npm run lint` are written for eslint; keeping one linter avoids two configs.                                                                                                                                                                                                       |
| D5  | 2026-08-29 | Indexability is decided at build time: `vite.config.ts` sets `import.meta.env.VITE_SITE_MODE` to `canonical` when `public/CNAME` exists, otherwise `preview`; `preview` renders `<meta name="robots" content="noindex">`. `robots.txt` and `sitemap.xml` are static and always describe the canonical host.                                                                                                                                                                                                                                                         | Crawlers must see the directive in HTML, not after a runtime hostname check; the CNAME file is already the single switch for the base path (§3.3).                                                                                                                                                           |
| D6  | 2026-08-29 | A minimal `public/404.html` ("Page not found" + link to the home page) is the only page-level copy not in the spec, alongside the form status messages authored in `ux-spec.md`.                                                                                                                                                                                                                                                                                                                                                                                    | GitHub Pages serves `404.html` for unknown paths; a one-line utility message is not marketing copy.                                                                                                                                                                                                          |
| D8  | 2026-08-29 | The FAQ section heading (not supplied by any approved source) is "Frequently Asked Questions" unless the seo-aeo-specialist's Phase 2c spec proposes a more specific wording that still contains no invented facts. Listed for the owner's optional review in the build report.                                                                                                                                                                                                                                                                                     | A utility heading, like the form status strings authored in `ux-spec.md`; the 8 Q&As themselves stay verbatim.                                                                                                                                                                                               |
| D9  | 2026-08-29 | Inquiry success panel: heading "Thanks, {name}!" and body = the approved auto-reply paragraph verbatim (ux-spec §6.4, "no repetition" option).                                                                                                                                                                                                                                                                                                                                                                                                                      | Avoids saying "We received your inquiry" twice in one panel; the visitor reads the same words on screen and in the auto-reply email.                                                                                                                                                                         |
| D10 | 2026-08-29 | Image-to-slot mapping follows the spec's filenames exactly (About = `about-still-life.jpeg`; Why trio includes `farm-zinnias.jpeg`). Alt text describes the actual content (ux-spec §7). The face-aware crop override in `build-images.mjs` applies to whichever file actually contains a person (`farm-zinnias.jpeg`), not to the About slot by name.                                                                                                                                                                                                              | The strategist found the filenames don't match their content; the spec's slot assignment is still the owner's choice and the visual reference agrees with it.                                                                                                                                                |
| D11 | 2026-08-29 | The packages footnote is set roman (small, muted) rather than italic as in the spec's markdown emphasis. No italic font files ship (≈130 KB saved) and a browser-synthesised italic would be faux. Copy unchanged.                                                                                                                                                                                                                                                                                                                                                  | Typographic rendering, not copy; performance budget (§3.4). Designer's request Q5.                                                                                                                                                                                                                           |
| D12 | 2026-08-29 | Design-spec deviations from ux-spec accepted as-is: Why trio at 4:5 "bucket frame" crops at all widths; form card on warm white (`surface-alt`) instead of cream; the packages CTA lives on the Custom band only. The About slot shows the file the spec names (`about-still-life.jpeg`, a vase still life); flagged to the owner in `docs/HANDOFF.md` and the build report in case an "at work" photo was intended.                                                                                                                                                | Visual calls are the designer's within the ux-spec's stated allowances; slot mapping is the owner's (D10).                                                                                                                                                                                                   |
| D13 | 2026-08-29 | The §3.4 "largest hero JPEG ≤ 160 KB" gate is restated as: largest hero **AVIF** ≤ 160 KB and the 768 px JPEG ≤ 160 KB (measured 137 KB and 122 KB). The 1152 px JPEG (266 KB at mozjpeg q78) is only the fallback for browsers with neither AVIF nor WebP and stays at the specified quality.                                                                                                                                                                                                                                                                      | Every current browser negotiates AVIF/WebP via `<picture>`; hitting 160 KB in JPEG needs ≈q55, which visibly softens the signage. Request Q7.                                                                                                                                                                |
| D14 | 2026-08-29 | The About slot's generated files are named `about-still-life-*` (SEO-4): the output name describes the content and carries no personal name. Source filename in the gitignored `assets-src/` is unchanged.                                                                                                                                                                                                                                                                                                                                                          | Public URLs should be accurate and impersonal (R10).                                                                                                                                                                                                                                                         |
| D7  | 2026-08-29 | The six agent definitions in `website/.claude/agents/` were written in Phase 1 but Claude Code only registers subagent types at session start, so this build session dispatches each role through the `general-purpose` agent with the plan's model override (`fable` for ux-strategist/ui-designer, `opus` for the rest) and instructs it to read its definition file as its operating instructions.                                                                                                                                                               | Same roles, models, tools and file ownership as the plan; no session restart needed. Future sessions started from `website/` will see the named agents directly.                                                                                                                                             |
| D15 | 2026-08-29 | R10 sweep: every reference to the owner by first name in repo docs/comments was replaced with "the owner"; the source Q&A file is cited as `owner-answers.md` (it lives outside the repo in the wrapper project's `1-genesis/…/questions/` folder) and the About original as `about-still-life.jpeg`.                                                                                                                                                                                                                                                               | The repo is public; stakeholder names stay out of it even in provenance notes. Request Q9.                                                                                                                                                                                                                   |
| D16 | 2026-08-29 | Inquiry success panel (supersedes D9): the verbatim auto-reply is rendered with its first sentence ("Thanks, {name}!") as the `<h3>` and the remainder as the paragraph, so the panel's reading order is byte-identical to the approved text and nothing repeats. `formCopy.ts` no longer carries a separate success heading.                                                                                                                                                                                                                                       | QA-2 / 5f-08: D9 as written duplicated the first sentence because the body is verbatim and also begins with it.                                                                                                                                                                                              |
| D17 | 2026-08-29 | Footer carries `© Happy Days Flower Farm` as a utility line (not marketing copy); no OSS license file in the public repo.                                                                                                                                                                                                                                                                                                                                                                                                                                           | Sean's §8 Q17 answer (all rights reserved so photos and copy are not implicitly licensed). QA nit N-1.                                                                                                                                                                                                       |
| D18 | 2026-08-29 | The orchestrator edited the QA harness directly (`scripts/lib/preview.mjs`, `lighthouse.sh`, the three QA scripts, `deploy.yml`) to fix a CI-only hang: `npx vite preview` killed via its wrapper left the real vite process orphaned on the Linux runner, holding the stderr pipe so `check-copy` never exited. Vite is now spawned from its bin in its own process group and the group is killed; each script exits explicitly; the build job has a 15-minute timeout.                                                                                            | Sean was waiting on a stuck 35-minute run; dispatching an agent would have cost more time than the fix. Verified locally with the CI base path and no orphan processes.                                                                                                                                      |
| D19 | 2026-08-29 | The Apps Script endpoint sends **no email**: every `MailApp` call (notification, auto-reply, daily quarantine digest, high-volume notice) is removed, and the manifest declares the single scope `spreadsheets.currentonly`. Notifications come from the spreadsheet's built-in _Tools → Notification settings_ rule (emails the Happy Days account on any change). The auto-reply is handled outside the site (an inbox rule) and is no longer a website deliverable; the approved auto-reply text is still shown on screen in the success panel (D16).            | Sean, 2026-08-29: a public endpoint must not hold a send-mail grant — the auto-reply was a backscatter vector and `script.send_mail` is the only way Apps Script sends mail. A row-append-only script is the smallest possible attack surface. Supersedes the §3.2 email layers (cap, digest) and §8 Q4/Q23. |
| D20 | 2026-08-29 | Notifications are out of scope for the site (supersedes the notification-rule part of D19). The endpoint's contract ends at "row saved to the sheet". Google Sheets notification rules cannot serve here: rules never fire for edits made by the rule-setting account, and the script runs as that same account (hello@); redeploying under another account is blocked for non-owner collaborators, and routing mail through a personal inbox was rejected as clutter. The owner will run a separate workflow (e.g. a scheduled Claude agent) that reads the sheet. | Sean, 2026-08-29. Keeps the endpoint at its minimum capability and avoids any mail permission or inbox forwarding.                                                                                                                                                                                           |
| D21 | 2026-08-29 | `<title>` changed from the spec's suggested "Happy Days Flower Farm \| Pop-Up Flower Bars in Western PA" to "Pop-Up Flower Bars in Pittsburgh, PA \| Happy Days Flower Farm" (`site.seo.title`; also `WebPage.name` and `og:title`). H1, description and all page copy unchanged.                                                                                                                                                                                                                                                                                   | Sean, 2026-08-29: "Western PA" is a weak local-search term; the city is what people search. Overrides the SEO spec's A2 "wait 90 days" recommendation — technical/SEO precedence is Sean's.                                                                                                                  |
