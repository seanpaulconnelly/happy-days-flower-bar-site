import { hero } from './content/copy';

/**
 * Phase 3 placeholder so the build is non-empty. Sections land in Phase 5;
 * all text comes from `src/content/copy.ts`, never inline.
 */
export default function App() {
  return (
    <main>
      <h1>{hero.heading}</h1>
    </main>
  );
}
